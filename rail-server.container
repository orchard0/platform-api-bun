[Container]
AutoUpdate=local
EnvironmentFile=.env.production

Image=rail-server.build
ContainerName=rail_server
HostName=rail_server
Network=net-rail-psql.network
Network=net-rail.network


PublishPort=6011:2030

# User=30
# UserNS=auto:size=32
# DropCapability=all
# AddCapability=CAP_NET_BIND_SERVICE

[Service]
Restart=always
# NoNewPrivileges=true # this cannot be used here

[Install]
WantedBy=default.target

[Unit]
After=rail-psql.service
BindsTo=rail-psql.service
